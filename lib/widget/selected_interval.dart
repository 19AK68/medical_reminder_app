import 'package:flutter/material.dart';

class SelectedInterval extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    String interval = "4";

    return Text(interval);
  }

}